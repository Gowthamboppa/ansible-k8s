---
 - name: CREATING THE K8S WORKSTATION
   hosts: all
   become: yes

  

   tasks:

   - name: DELETING CLUSTER
     shell: /usr/local/bin/kops delete cluster --name=boppa.k8s.local --yes  --state=s3://sindhu123456   
 
          