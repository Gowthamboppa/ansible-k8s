---
 - name: CREATING THE K8S WORKSTATION
   hosts: all
   become: yes

  

   tasks:

   - name: Create delete cluster
     shell: /usr/local/bin/kops delete cluster --name=boppa.k8s.local --yes  --state=s3://sindhu123456  --ssh-public-key /home/centos/.ssh/id_rsa.pub 
 
          