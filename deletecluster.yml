---
 - name: CREATING THE K8S WORKSTATION
   hosts: all
   become: yes

  

   tasks:

   - name: Create delete cluster
     shell: /usr/local/bin/kops delete cluster --yes --name=boppa.k8s.local --state=s3://sindhu123456  --ssh-public-key /home/centos/.ssh/id_rsa.pub 
 
          